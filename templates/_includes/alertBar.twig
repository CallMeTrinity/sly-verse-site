{% if alertBar is defined and alertBar.alertBarStartDate < now and alertBar.alertBarEndDate > now %}
	<div class="bg-blue py-2 text-center text-sm font-bold text-white">
		<article class="prose mx-auto">
			{{ alertBar.alertBarText }}
		</article>
	</div>
{% endif %}
